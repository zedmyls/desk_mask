# Desk Mask（桌面遮罩）

## 目标

为 macOS 与 Windows 11 提供一个全屏、始终置顶、鼠标可穿透的颜色遮罩。它通过颜色和透明度降低视觉刺激；第一版不尝试改变显示器物理背光，也不针对 HDR 的色彩合成进行校准。

## 第一版（V1）范围

### 遮罩

- 为每块已启用的显示器创建独立的全屏遮罩。
- 遮罩始终显示在普通应用之上，没有边框，不拦截鼠标点击。
- 可选择任意遮罩颜色；默认使用黑色。
- 可用 0–100% 的透明度调节强度，调节时立即预览；可设置 0–50% 的透明度下限以避免遮罩过深。
- 可从主窗口启用、隐藏或切换遮罩。

### 配置

- 内置“夜间柔和”“暖色阅读”两个示例预设。
- 可新增、保存、应用和删除预设，每个预设保存颜色与不透明度。
- 配置保存在本机用户目录，应用重启后继续可用。
- 可选择登录后自动启动应用，且可设置启动后是否自动显示遮罩。

### 平台与实现

- 使用 Tauri 2：前端采用 TypeScript/CSS，窗口和系统集成采用 Rust。
- macOS：使用原生窗口层级、全屏与鼠标穿透能力。
- 为让 macOS 遮罩窗口支持透明背景，启用 Tauri 的 `macos-private-api` 特性；因此发布渠道定位为官网/开发者签名分发，而非 Mac App Store。
- Windows 11：使用同一套 Tauri 窗口 API；需要在 Windows 实机检查置顶、全屏独占程序和多显示器边界行为。
- 遮罩是一层视觉叠加，无法调低面板背光；黑色半透明遮罩通常比灰色遮罩更接近“降亮度”的感觉。

## 不在 V1 的范围

- HDR 颜色空间、峰值亮度和色调映射校准。
- 通过 DDC/CI 调节显示器物理亮度。
- 针对游戏、DRM 视频或其他独占全屏内容的强制覆盖。
- 云同步与跨设备同步预设。

## 升级路线

### V1.1：日常使用体验

- 为每台显示器记忆单独的遮罩状态。
- 增加可配置的全局快捷键、默认预设和定时启停。
- 增加菜单栏/系统托盘操作，以及导入与导出预设。

### V2：显示器控制（可选）

- 检测支持 DDC/CI 的外接显示器并提供物理亮度调节。
- 保持遮罩模式作为不支持 DDC/CI、或希望保留面板亮度时的替代方案。

### V3：Windows HDR 专项适配

- 在 Windows 的 HDR 合成路径上测试并校准遮罩的颜色和透明度。
- 必要时使用 HDR 感知的 DirectX/DirectComposition 渲染层，减少灰雾、色偏及高光处理异常。
- 将 HDR 模式作为独立选项，而非改变普通遮罩的稳定行为。

## 验收标准

在普通 SDR 桌面上，用户能够在不中断鼠标与键盘操作的前提下，实时改变全屏遮罩的颜色和强度，保存多个预设，并让应用随登录自动运行。
